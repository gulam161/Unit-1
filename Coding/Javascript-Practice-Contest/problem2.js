function abc(a, b, c, d, e, f) {
  let x = Math.pow(a, b);
  console.log(x)
  let y = c + e;
  let z = d * f;

  let equation = 2 * (x) + 5 * (y) + 10 * z;
  console.log(equation);
}
abc(5, 7, 90, 60, 20, 10);